{
  "$schema": {{{json schemaUrl}}},
  "description": {{{json description}}},
  "name": {{{json name}}},
  "packages": {{{json packages}}},
  "repository": {{{json repository}}},
  "version": {{{json version}}}{{#if websiteUrl}},
  "websiteUrl": {{{json websiteUrl}}}{{/if}}
}
