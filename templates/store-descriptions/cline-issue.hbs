### GitHub Repository URL

{{repo_url}}

### Logo Image

![logo]({{logo_url}})

### Installation Testing

- [x] The server installs via the documented `npx` / Docker commands and has been tested end-to-end with MCP clients (Claude, n8n)
- [x] The server is stable and in production use (published on npm and Docker Hub)

### Additional Information

{{description}}

**For Cline users — `cline_mcp_settings.json` block to install via npx:**

```json
{
  "mcpServers": {
    "{{mcp_name}}": {
      "command": "npx",
      "args": ["-y", "{{npm_package_name}}"],
      "env": {
{{#each environment_variables}}
        "{{name}}": "<your-{{name}}>"{{#unless @last}},{{/unless}}
{{/each}}
      }
    }
  }
}
```

Alternative install via Docker:

```bash
docker run --rm -it --env-file .env {{docker_image_name}}:{{version}}
```

**Repository status:** {{license}} license · v{{version}} just shipped · published on npm (with OIDC provenance), Docker Hub, MCP Official Registry, Smithery, and n8n community node. Maintained by g-digital by Garrigues (legal-tech / digital-trust vendor; J&A Garrigues, S.L.P.).

<!-- pipeline-run-id: {{pipeline_run_id}} -->
