name: publish

# Triggered by:
#   1. Engineer pushes a v* tag created by /prep-mcp (Story 1.11), or
#   2. Engineer manually dispatches from the Actions UI for re-runs and dry-runs.
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      mcp_name:
        description: "MCP name (kebab-case key in mcp-pipeline.yaml#mcps)"
        required: true
        type: string
      version:
        description: "Semver to publish (e.g., 1.0.0)"
        required: true
        type: string
      step:
        description: "Which target/phase to run. 'gate'/'publish'/'all' OR a single target ID (npm, cline, etc.)"
        required: false
        type: string
        default: all
      track:
        description: "Track(s) to run"
        required: false
        type: choice
        options:
          - a
          - b
          - both
        default: both
      bump:
        description: "Optional explicit bump override (empty = derive from commits)"
        required: false
        type: choice
        options:
          - ""
          - patch
          - minor
          - major
        default: ""
      dry_run:
        description: "Dry-run mode (skip actual publish actions)"
        required: false
        type: boolean
        default: false

# `cancel-in-progress: false` is deliberate: a publish in flight must not be
# pre-empted by a follow-up tag — we'd rather queue and process serially per
# (mcp_name, version) tuple. For tag pushes the group falls back to the tag
# name itself (which is already unique per release), so two unrelated tag
# pushes don't block each other.
concurrency:
  group: publish-${{ inputs.mcp_name || github.ref_name }}-${{ inputs.version || github.ref_name }}
  cancel-in-progress: false

jobs:
  setup:
    name: Resolve context + verify build
    runs-on: ubuntu-latest
    outputs:
      mcp_name: ${{ steps.resolve.outputs.mcp_name }}
      version: ${{ steps.resolve.outputs.version }}
      pipeline_run_id: ${{ steps.resolve.outputs.pipeline_run_id }}
      source: ${{ steps.resolve.outputs.source }}
      dry_run: ${{ steps.resolve.outputs.dry_run }}
      # Per-MCP source repo (v1.1 per-MCP-repo refactor — Phase B). Empty
      # string when the MCP hasn't migrated yet (back-compat: pipeline
      # then uses the copy committed under pending-to-publish/<id>/).
      repo_url: ${{ steps.resolve-mcp-repo.outputs.repo_url }}
      # Tag (v<version>) for real publishes; 'main' for dry-runs.
      repo_ref: ${{ steps.resolve-mcp-repo.outputs.repo_ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Resolve workflow context
        id: resolve
        env:
          GH_REF_TYPE: ${{ github.ref_type }}
          GH_REF_NAME: ${{ github.ref_name }}
          GH_RUN_ID: ${{ github.run_id }}
          GH_RUN_ATTEMPT: ${{ github.run_attempt }}
          INPUT_MCP_NAME: ${{ inputs.mcp_name }}
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail
          pnpm tsx src/ci/resolve-workflow-context.ts > resolved.json
          mcp_name=$(node -p "JSON.parse(require('fs').readFileSync('resolved.json','utf8')).mcp_name")
          version=$(node -p "JSON.parse(require('fs').readFileSync('resolved.json','utf8')).version")
          pipeline_run_id=$(node -p "JSON.parse(require('fs').readFileSync('resolved.json','utf8')).pipeline_run_id")
          source=$(node -p "JSON.parse(require('fs').readFileSync('resolved.json','utf8')).source")
          # Tag pushes never carry a dry_run input; fall back to "false" so
          # downstream publishers can treat this output as a stable boolean.
          dry_run_raw="${INPUT_DRY_RUN:-false}"
          case "$(printf '%s' "$dry_run_raw" | tr '[:upper:]' '[:lower:]')" in
            true|1|yes) dry_run=true ;;
            *) dry_run=false ;;
          esac
          echo "mcp_name=$mcp_name" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "pipeline_run_id=$pipeline_run_id" >> "$GITHUB_OUTPUT"
          echo "source=$source" >> "$GITHUB_OUTPUT"
          echo "dry_run=$dry_run" >> "$GITHUB_OUTPUT"
          {
            echo "## Resolved context"
            echo "- mcp_name: \`$mcp_name\`"
            echo "- version: \`$version\`"
            echo "- pipeline_run_id: \`$pipeline_run_id\`"
            echo "- source: \`$source\`"
            echo "- dry_run: \`${{ inputs.dry_run }}\`"
            echo "- step: \`${{ inputs.step || 'all' }}\`"
            echo "- track: \`${{ inputs.track || 'both' }}\`"
          } >> "$GITHUB_STEP_SUMMARY"

      # Resolve the MCP's own source repo (if it has migrated to its own
      # repo per v1.1 Phase B). Empty repo_url is fine: downstream jobs
      # then keep using the committed pending-to-publish/<id>/ copy.
      # repo_ref = 'v<version>' on real publishes, 'main' on dry-runs.
      - name: Resolve MCP source repo
        id: resolve-mcp-repo
        run: |
          set -euo pipefail
          REPO_URL=$(yq -r ".mcps[\"${{ steps.resolve.outputs.mcp_name }}\"].repo_url // \"\"" mcp-pipeline.yaml)
          if [ "${{ steps.resolve.outputs.dry_run }}" = "true" ]; then
            REPO_REF="main"
          else
            REPO_REF="v${{ steps.resolve.outputs.version }}"
          fi
          echo "repo_url=$REPO_URL" >> "$GITHUB_OUTPUT"
          echo "repo_ref=$REPO_REF" >> "$GITHUB_OUTPUT"
          if [ -n "$REPO_URL" ]; then
            echo "Per-MCP source repo: $REPO_URL @ $REPO_REF"
          else
            echo "No per-MCP repo configured for ${{ steps.resolve.outputs.mcp_name }}; pipeline will use the copy under pending-to-publish/${{ steps.resolve.outputs.mcp_name }}/."
          fi

      - name: Verify the build still compiles on the runner
        run: pnpm run typecheck && pnpm run test

  # ---------------------------------------------------------------------
  # Story 4.8: read the releases/state ledger so each publisher can be
  # skipped when this same (mcp, version) tuple already has it at
  # status='succeeded'. Output: per-target run_<id> = 'true' | 'false'.
  # ---------------------------------------------------------------------

  ledger-read:
    name: Read release-state ledger
    needs: setup
    runs-on: ubuntu-latest
    outputs:
      run_npm: ${{ steps.read.outputs.run_npm }}
      run_docker_hub: ${{ steps.read.outputs.run_docker_hub }}
      run_mcp_publisher: ${{ steps.read.outputs.run_mcp_publisher }}
      run_smithery: ${{ steps.read.outputs.run_smithery }}
      run_docker_mcp_catalog: ${{ steps.read.outputs.run_docker_mcp_catalog }}
      run_cline: ${{ steps.read.outputs.run_cline }}
      run_mcpso: ${{ steps.read.outputs.run_mcpso }}
      run_n8n: ${{ steps.read.outputs.run_n8n }}
      run_make_rom: ${{ steps.read.outputs.run_make_rom }}
      ledger_json: ${{ steps.read.outputs.ledger_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      MCP_NAME: ${{ needs.setup.outputs.mcp_name }}
      VERSION: ${{ needs.setup.outputs.version }}
      # Pass the raw inputs.step / inputs.track through. The read-ledger
      # script interprets phase keywords ('gate'/'publish'/'all'/'') and
      # the 'both' track value — doing the conditional in YAML hit the
      # GH Actions expression footgun where `a && '' || b` evaluates to
      # `b` because '' is falsy (caught in dry-run run 25853475366).
      RETRY_STEP: ${{ inputs.step }}
      RETRY_TRACK: ${{ inputs.track }}
      BOT_PAT: ${{ secrets.BOT_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # Required so read-ledger.ts can load skip_targets from the MCP's
      # .distribution.yaml (Phase C of v1.1 moved per-MCP fields out of
      # this repo's mcp-pipeline.yaml). Without this step, the loader
      # silently fails and the skip filter is never applied — see the
      # bug observed in run #25893xxx where publish-smithery executed
      # despite skip_targets: [smithery].
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install pipeline dependencies
        run: pnpm install --frozen-lockfile
      - name: Read ledger from releases/state branch
        id: read
        run: pnpm tsx src/state/read-ledger.ts

  nfr-s3-audit:
    name: NFR-S3 audit (no consumer credentials in CI)
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run consumer-credentials audit
        run: pnpm tsx src/ci/audit-consumer-credentials.ts

  track-a-layer-1:
    name: Track A — Layer 1 (static validation)
    needs: setup
    runs-on: ubuntu-latest
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Track A Layer 1 gate
        run: |
          set -euo pipefail
          pnpm tsx src/gates/run-track-a-layer-1.ts "${{ needs.setup.outputs.mcp_name }}" \
            | tee gate-layer-1.json

      - name: Upload gate report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: track-a-layer-1-report
          path: gate-layer-1.json
          if-no-files-found: warn

  track-a-layer-2:
    name: Track A — Layer 2 (MCP protocol smoke test)
    needs: [setup, track-a-layer-1]
    runs-on: ubuntu-latest
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install pipeline dependencies
        run: pnpm install --frozen-lockfile

      - name: Install + build the MCP source
        working-directory: pending-to-publish/${{ needs.setup.outputs.mcp_name }}
        run: |
          set -euo pipefail
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install --no-audit --no-fund
          fi
          npm run build

      - name: Run Track A Layer 2 gate
        run: |
          set -euo pipefail
          pnpm tsx src/gates/run-track-a-layer-2.ts "${{ needs.setup.outputs.mcp_name }}" \
            | tee gate-layer-2.json

      - name: Upload gate report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: track-a-layer-2-report
          path: gate-layer-2.json
          if-no-files-found: warn

  track-a-layer-3:
    name: Track A — Layer 3 (build & install)
    needs: [setup, track-a-layer-2]
    runs-on: ubuntu-latest
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      DOCKER_BUILDKIT: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}

      - name: Set up Docker Buildx (for BuildKit + layer caching)
        uses: docker/setup-buildx-action@v3

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install pipeline dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Track A Layer 3 gate
        run: |
          set -euo pipefail
          pnpm tsx src/gates/run-track-a-layer-3.ts "${{ needs.setup.outputs.mcp_name }}" \
            | tee gate-layer-3.json

      - name: Upload gate report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: track-a-layer-3-report
          path: gate-layer-3.json
          if-no-files-found: warn

  gate-failure-summary:
    name: Gate failure summary (aggregate + PR comment)
    needs: [setup, track-a-layer-1, track-a-layer-2, track-a-layer-3]
    if: >-
      always() &&
      (needs.track-a-layer-1.result == 'failure' ||
       needs.track-a-layer-2.result == 'failure' ||
       needs.track-a-layer-3.result == 'failure')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install pipeline dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Layer 1 report
        if: needs.track-a-layer-1.result != 'skipped'
        uses: actions/download-artifact@v4
        with:
          name: track-a-layer-1-report
          path: reports/layer-1
        continue-on-error: true

      - name: Download Layer 2 report
        if: needs.track-a-layer-2.result != 'skipped'
        uses: actions/download-artifact@v4
        with:
          name: track-a-layer-2-report
          path: reports/layer-2
        continue-on-error: true

      - name: Download Layer 3 report
        if: needs.track-a-layer-3.result != 'skipped'
        uses: actions/download-artifact@v4
        with:
          name: track-a-layer-3-report
          path: reports/layer-3
        continue-on-error: true

      - name: Aggregate error reports
        run: |
          set -euo pipefail
          reports=()
          for f in reports/layer-1/gate-layer-1.json reports/layer-2/gate-layer-2.json reports/layer-3/gate-layer-3.json; do
            if [ -f "$f" ]; then reports+=("$f"); fi
          done
          if [ ${#reports[@]} -eq 0 ]; then
            echo '{"pipeline_run_id":"'"$PIPELINE_RUN_ID"'","mcpName":null,"has_failures":true,"source_reports":[],"errors":[]}' > error-report.json
          else
            pnpm tsx src/ci/aggregate-error-reports.ts "${reports[@]}" --pipeline-run-id "$PIPELINE_RUN_ID" > error-report.json
          fi
          cp error-report.json "error-report-${PIPELINE_RUN_ID}.json"

      - name: Render markdown comment body
        env:
          WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          pnpm tsx src/ci/render-error-report-markdown.ts error-report.json --workflow-run-url "$WORKFLOW_RUN_URL" > error-report.md

      - name: Upload aggregated error report
        uses: actions/upload-artifact@v4
        with:
          name: error-report-${{ env.PIPELINE_RUN_ID }}
          path: error-report-${{ env.PIPELINE_RUN_ID }}.json
          if-no-files-found: error

      - name: Post or update PR comment
        uses: actions/github-script@v7
        env:
          PIPELINE_RUN_ID: ${{ env.PIPELINE_RUN_ID }}
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('error-report.md', 'utf8');
            const marker = `<!-- error-report:${process.env.PIPELINE_RUN_ID} -->`;

            // Find the PR(s) associated with the commit that produced this run.
            const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
            });
            const pr = prs[0];
            if (!pr) {
              core.notice('No PR associated with this commit; error-report posted only as artifact.');
              core.summary.addRaw(body).write();
              return;
            }

            // Update an existing comment with the same marker, otherwise create one.
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              per_page: 100,
            });
            const existing = comments.find((c) => c.body && c.body.startsWith(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
              core.notice(`Updated existing error-report comment on PR #${pr.number}.`);
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr.number,
                body,
              });
              core.notice(`Posted error-report comment on PR #${pr.number}.`);
            }
            core.summary.addRaw(body).write();

  # ---------------------------------------------------------------------
  # Track A core publishers (Stories 3.2 / 3.3 / 3.4)
  # ---------------------------------------------------------------------

  publish-npm:
    name: Publish to npm
    needs: [setup, ledger-read, track-a-layer-3]
    if: |
      needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_npm == 'true'
    runs-on: ubuntu-latest
    permissions:
      # id-token: write enables the OIDC trusted-publisher path. NPM_TOKEN
      # still takes precedence when set (Story 3.2's first-publish path).
      contents: read
      id-token: write
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-npm
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  publish-docker-hub:
    name: Publish to Docker Hub
    needs: [setup, ledger-read, track-a-layer-3]
    if: |
      needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_docker_hub == 'true'
    runs-on: ubuntu-latest
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-docker-hub
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  publish-mcp-registry:
    name: Publish to MCP Official Registry
    # mcp-registry depends on publish-npm because the registry's package-
    # ownership verification reads the mcpName field from the npm package.
    # We need the package to be live first. Use always() so a failed npm
    # publish doesn't silently skip this — the dependency expresses order,
    # not blocking semantics, and the conditional below adds the gate.
    #
    # The `result == 'skipped'` branch handles the retry-only case (e.g.
    # /retry-publish?step=mcp-publisher) where publish-npm is skipped
    # because the ledger says npm already succeeded for this version.
    # Without this branch, the `contains(result_json, succeeded)` check
    # would treat that as "npm failed" (because result_json is empty and
    # falls back to '{"status":"failed"}') and skip mcp-registry too.
    needs: [setup, ledger-read, track-a-layer-3, publish-npm]
    if: |
      always()
      && needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_mcp_publisher == 'true'
      && (needs.publish-npm.result == 'skipped'
          || contains(fromJson(needs.publish-npm.outputs.result_json || '{"status":"failed"}').status, 'succeeded'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-mcp-registry
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  # ---------------------------------------------------------------------
  # Epic 4 publishers (Stories 4.2 / 4.3 / 4.4 / 4.5)
  # Independent of each other — run in parallel after the gates pass.
  # All 4 are non-blocking for the npm/Docker/MCP-Registry "core 3" path.
  # ---------------------------------------------------------------------

  publish-smithery:
    name: Publish to Smithery
    needs: [setup, ledger-read, track-a-layer-3]
    if: |
      needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_smithery == 'true'
    runs-on: ubuntu-latest
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-smithery
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  publish-docker-mcp-catalog:
    name: Publish to Docker MCP Catalog
    # Depends on publish-npm because the catalog server.yaml references
    # the npm package and the issue body uses unpkg.com URLs (logo,
    # install command). Those URLs don't resolve until the npm package
    # is actually published.
    #
    # The `result == 'skipped'` branch covers retry-only dispatches where
    # publish-npm is skipped because the ledger already records it as
    # succeeded for this version (run #25869794563 retry — caught us).
    needs: [setup, ledger-read, track-a-layer-3, publish-npm]
    if: |
      always()
      && needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_docker_mcp_catalog == 'true'
      && (needs.publish-npm.result == 'skipped'
          || contains(fromJson(needs.publish-npm.outputs.result_json || '{"status":"failed"}').status, 'succeeded'))
    runs-on: ubuntu-latest
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      BOT_PAT: ${{ secrets.BOT_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-docker-mcp-catalog
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  publish-cline:
    name: Publish to Cline Marketplace
    # Depends on publish-npm so the unpkg.com logo URL in the issue body
    # actually resolves when Cline maintainers review.
    #
    # The `result == 'skipped'` branch covers retry-only dispatches (see
    # publish-mcp-registry above).
    needs: [setup, ledger-read, track-a-layer-3, publish-npm]
    if: |
      always()
      && needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_cline == 'true'
      && (needs.publish-npm.result == 'skipped'
          || contains(fromJson(needs.publish-npm.outputs.result_json || '{"status":"failed"}').status, 'succeeded'))
    runs-on: ubuntu-latest
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      BOT_PAT: ${{ secrets.BOT_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-cline
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  publish-mcpso:
    name: Publish to mcp.so
    # Depends on publish-npm so the unpkg.com logo URL in the issue body
    # actually resolves when mcp.so maintainers review.
    #
    # The `result == 'skipped'` branch covers retry-only dispatches (see
    # publish-mcp-registry above).
    needs: [setup, ledger-read, track-a-layer-3, publish-npm]
    if: |
      always()
      && needs.track-a-layer-3.result == 'success'
      && needs.ledger-read.outputs.run_mcpso == 'true'
      && (needs.publish-npm.result == 'skipped'
          || contains(fromJson(needs.publish-npm.outputs.result_json || '{"status":"failed"}').status, 'succeeded'))
    runs-on: ubuntu-latest
    outputs:
      result_json: ${{ steps.publish.outputs.result_json }}
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      BOT_PAT: ${{ secrets.BOT_PAT }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Checkout MCP source
        uses: ./actions/checkout-mcp-source
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          repo_url: ${{ needs.setup.outputs.repo_url }}
          repo_ref: ${{ needs.setup.outputs.repo_ref }}
      - name: Publish via composite action
        id: publish
        uses: ./actions/publish-mcpso
        with:
          mcp_name: ${{ needs.setup.outputs.mcp_name }}
          version: ${{ needs.setup.outputs.version }}
          pipeline_run_id: ${{ needs.setup.outputs.pipeline_run_id }}
          dry_run: ${{ needs.setup.outputs.dry_run }}

  # ---------------------------------------------------------------------
  # Story 3.8: npx install-path verification (FR47).
  # Runs `npx -y @g-digital/mcp-<name>@<version>` as a fresh process and
  # does the MCP initialize + tools/list handshake to confirm the
  # consumer install path actually works. Gated on publish-npm success
  # so we never npx-install a version that didn't actually publish.
  # ---------------------------------------------------------------------

  npx-verification:
    name: npx install-path verification
    needs: [setup, publish-npm]
    if: |
      needs.publish-npm.result == 'success'
      && needs.setup.outputs.dry_run != 'true'
      && fromJson(needs.publish-npm.outputs.result_json).status == 'succeeded'
    runs-on: ubuntu-latest
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install pipeline dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify npx install path (initialize + tools/list)
        run: |
          set -euo pipefail
          pnpm tsx src/ci/verify-npx-install.ts \
            "${{ needs.setup.outputs.mcp_name }}" \
            "${{ needs.setup.outputs.version }}" \
            | tee npx-verification.json

      - name: Upload verification report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: npx-verification-${{ needs.setup.outputs.mcp_name }}-v${{ needs.setup.outputs.version }}
          path: npx-verification.json
          if-no-files-found: warn

  # ---------------------------------------------------------------------
  # Story 3.7: final-report job — aggregates Track A publishers,
  # commits the release report, and upserts the PR comment.
  # Always runs so partial-publish + full-failure cases still get a report.
  # ---------------------------------------------------------------------

  final-report:
    name: Final release report
    needs:
      - setup
      - publish-npm
      - publish-docker-hub
      - publish-mcp-registry
      - publish-smithery
      - publish-docker-mcp-catalog
      - publish-cline
      - publish-mcpso
    if: always() && needs.setup.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write        # commit the markdown report
      pull-requests: write   # upsert the PR comment
    env:
      PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
      DRY_RUN: ${{ needs.setup.outputs.dry_run }}
      MCP_NAME: ${{ needs.setup.outputs.mcp_name }}
      VERSION: ${{ needs.setup.outputs.version }}
      WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # We need full history so the commit-and-push step can rebase
          # against any concurrent commits on main / the PR branch.
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install pipeline dependencies
        run: pnpm install --frozen-lockfile

      - name: Render release report
        env:
          # Fallback differentiates between:
          #   - job didn't run AND was explicitly skipped (skip_targets, or
          #     'if' returned false) → status=skipped (no error block,
          #     report stays ✅ Complete)
          #   - job didn't run because an upstream FAILED → status=failed
          #     with the cascade message (report becomes ⚠️ Partial)
          # GH Actions exposes the job state via `needs.<job>.result`,
          # which is 'success' | 'failure' | 'cancelled' | 'skipped'. If
          # result_json IS present we use that; otherwise we synthesize
          # a row based on result.
          PUBLISHER_RESULTS_JSON: |
            ${{ needs.publish-npm.outputs.result_json || (needs.publish-npm.result == 'skipped' && format('{{"target":"npm","status":"skipped","target_url":"https://example.invalid/skipped/npm","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"npm","status":"failed","target_url":"https://example.invalid/skipped/npm","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-npm job did not run (skipped or pre-empted by upstream failure).","cause":"Upstream gate or dependency was not successful.","action":"Inspect the workflow run; re-run after fixing the upstream gate."}}}}', needs.setup.outputs.dry_run)) }}
            ${{ needs.publish-docker-hub.outputs.result_json || (needs.publish-docker-hub.result == 'skipped' && format('{{"target":"docker-hub","status":"skipped","target_url":"https://example.invalid/skipped/docker-hub","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"docker-hub","status":"failed","target_url":"https://example.invalid/skipped/docker-hub","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-docker-hub job did not run.","cause":"Upstream gate or dependency was not successful.","action":"Inspect the workflow run; re-run after fixing the upstream gate."}}}}', needs.setup.outputs.dry_run)) }}
            ${{ needs.publish-mcp-registry.outputs.result_json || (needs.publish-mcp-registry.result == 'skipped' && format('{{"target":"mcp-publisher","status":"skipped","target_url":"https://example.invalid/skipped/mcp-publisher","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"mcp-publisher","status":"failed","target_url":"https://example.invalid/skipped/mcp-publisher","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-mcp-registry job did not run.","cause":"Upstream npm publish did not succeed.","action":"Re-run /retry-publish?step=mcp-publisher after npm succeeds."}}}}', needs.setup.outputs.dry_run)) }}
            ${{ needs.publish-smithery.outputs.result_json || (needs.publish-smithery.result == 'skipped' && format('{{"target":"smithery","status":"skipped","target_url":"https://example.invalid/skipped/smithery","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"smithery","status":"failed","target_url":"https://example.invalid/skipped/smithery","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-smithery job did not run.","cause":"Upstream gate or dependency was not successful.","action":"Inspect the workflow run; re-run after fixing the upstream gate."}}}}', needs.setup.outputs.dry_run)) }}
            ${{ needs.publish-docker-mcp-catalog.outputs.result_json || (needs.publish-docker-mcp-catalog.result == 'skipped' && format('{{"target":"docker-mcp-catalog","status":"skipped","target_url":"https://example.invalid/skipped/docker-mcp-catalog","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"docker-mcp-catalog","status":"failed","target_url":"https://example.invalid/skipped/docker-mcp-catalog","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-docker-mcp-catalog job did not run.","cause":"Upstream gate or dependency was not successful.","action":"Inspect the workflow run; re-run after fixing the upstream gate."}}}}', needs.setup.outputs.dry_run)) }}
            ${{ needs.publish-cline.outputs.result_json || (needs.publish-cline.result == 'skipped' && format('{{"target":"cline","status":"skipped","target_url":"https://example.invalid/skipped/cline","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"cline","status":"failed","target_url":"https://example.invalid/skipped/cline","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-cline job did not run.","cause":"Upstream gate or dependency was not successful.","action":"Inspect the workflow run; re-run after fixing the upstream gate."}}}}', needs.setup.outputs.dry_run)) }}
            ${{ needs.publish-mcpso.outputs.result_json || (needs.publish-mcpso.result == 'skipped' && format('{{"target":"mcpso","status":"skipped","target_url":"https://example.invalid/skipped/mcpso","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0}}}', needs.setup.outputs.dry_run) || format('{{"target":"mcpso","status":"failed","target_url":"https://example.invalid/skipped/mcpso","version_published":null,"duration_ms":0,"attempts":1,"dry_run":{0},"error":{{"message":"publish-mcpso job did not run.","cause":"Upstream gate or dependency was not successful.","action":"Inspect the workflow run; re-run after fixing the upstream gate."}}}}', needs.setup.outputs.dry_run)) }}
        run: |
          set -euo pipefail
          pnpm tsx src/ci/render-release-report.ts > /dev/null

      - name: Upload release-report artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-report-${{ needs.setup.outputs.mcp_name }}-v${{ needs.setup.outputs.version }}
          path: _bmad-output/release-reports/${{ needs.setup.outputs.mcp_name }}-v${{ needs.setup.outputs.version }}.md
          if-no-files-found: error

      - name: Write release-state ledger (Story 4.8)
        # Merge this run's PublisherOutputs into releases/state/<mcp>/<version>.json
        # on the orphan branch so the next retry knows which targets are
        # already done. Skipped in dry-run; non-fatal if it fails so a
        # ledger-push transient doesn't make the whole release look broken.
        if: needs.setup.outputs.dry_run != 'true'
        continue-on-error: true
        env:
          MCP_NAME: ${{ needs.setup.outputs.mcp_name }}
          VERSION: ${{ needs.setup.outputs.version }}
          PIPELINE_RUN_ID: ${{ needs.setup.outputs.pipeline_run_id }}
          BOT_PAT: ${{ secrets.BOT_PAT }}
          PUBLISHER_RESULTS_JSON: |
            ${{ needs.publish-npm.outputs.result_json || '' }}
            ${{ needs.publish-docker-hub.outputs.result_json || '' }}
            ${{ needs.publish-mcp-registry.outputs.result_json || '' }}
            ${{ needs.publish-smithery.outputs.result_json || '' }}
            ${{ needs.publish-docker-mcp-catalog.outputs.result_json || '' }}
            ${{ needs.publish-cline.outputs.result_json || '' }}
            ${{ needs.publish-mcpso.outputs.result_json || '' }}
        run: pnpm tsx src/state/write-ledger.ts

      - name: Commit + push release-report to main
        # NEVER commit in dry-run mode; the workflow artifact above is the
        # only output. Same when there is no PR (manual dispatch off main)
        # but we still want to land the file on main.
        if: needs.setup.outputs.dry_run != 'true'
        env:
          REPORT_PATH: _bmad-output/release-reports/${{ needs.setup.outputs.mcp_name }}-v${{ needs.setup.outputs.version }}.md
        run: |
          set -euo pipefail
          git config user.name "g-digital-mcp-bot"
          git config user.email "g-digital-mcp-bot@users.noreply.github.com"
          git add "$REPORT_PATH"
          if git diff --staged --quiet; then
            echo "No changes to release report — nothing to commit."
            exit 0
          fi
          git commit -m "docs(release): ${MCP_NAME} v${VERSION} release report"
          # Rebase any concurrent commits in case another run landed
          # something while this one was in flight.
          git pull --rebase origin "${GITHUB_REF_NAME}" || git pull --rebase origin main
          git push

      - name: Upsert PR comment with release report
        if: needs.setup.outputs.dry_run != 'true'
        uses: actions/github-script@v7
        env:
          MCP_NAME: ${{ needs.setup.outputs.mcp_name }}
          VERSION: ${{ needs.setup.outputs.version }}
        with:
          script: |
            const fs = require('fs');
            const mcpName = process.env.MCP_NAME;
            const version = process.env.VERSION;
            const reportPath = `_bmad-output/release-reports/${mcpName}-v${version}.md`;
            const body = fs.readFileSync(reportPath, 'utf8');
            const marker = `<!-- release-report:${mcpName}-v${version} -->`;

            const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
            });
            const pr = prs[0];
            if (!pr) {
              core.notice('No PR associated with this commit; release report posted only as artifact + main commit.');
              core.summary.addRaw(body).write();
              return;
            }

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              per_page: 100,
            });
            const existing = comments.find((c) => c.body && c.body.includes(marker));
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
              core.notice(`Updated release-report comment on PR #${pr.number}.`);
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr.number,
                body,
              });
              core.notice(`Posted release-report comment on PR #${pr.number}.`);
            }
            core.summary.addRaw(body).write();
