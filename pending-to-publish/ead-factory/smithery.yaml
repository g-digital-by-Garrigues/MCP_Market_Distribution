# Smithery configuration (v1) — generated by generate-smithery-yaml.ts.
# Do not edit by hand. Regenerate from mcp-pipeline.yaml + the source .env.example.
configSchema:
  $schema: https://json-schema.org/draft/2020-12/schema
  properties:
    API_BASE_URL:
      description: Evidence Manager API base URL
      type: string
    FULL_FLOW_EMAIL_BASE:
      description: Full flow base email — used to compose participant emails (user+signatory@domain, etc.)
      type: string
    FULL_FLOW_FILE_PATH:
      description: Full flow default file path
      type: string
    HTTP_PORT:
      description: HTTP_PORT
      type: string
    OKTA_CLIENT_ID:
      description: OKTA_CLIENT_ID
      type: string
    OKTA_CLIENT_SECRET:
      description: OKTA_CLIENT_SECRET (See https://eadtrust.example.com/onboarding for credential acquisition.)
      format: password
      type: string
    OKTA_SCOPE:
      description: OKTA_SCOPE
      type: string
    OKTA_TOKEN_URL:
      description: OAuth credentials (Okta client_credentials flow) Used both for calling Evidence Manager API and for verifying incoming Bearer tokens (HTTP mode)
      type: string
    POLL_INTERVAL_MS:
      description: Polling configuration for evidence status
      type: string
    POLL_MAX_ATTEMPTS:
      description: POLL_MAX_ATTEMPTS
      type: string
    SIGNATURE_API_BASE_URL:
      description: Signature Manager API base URL
      type: string
    TRANSPORT:
      description: "Transport: \"stdio\" for local Claude Code, \"http\" for remote deployment with auth"
      type: string
  required:
    - API_BASE_URL
    - FULL_FLOW_EMAIL_BASE
    - FULL_FLOW_FILE_PATH
    - HTTP_PORT
    - OKTA_CLIENT_ID
    - OKTA_CLIENT_SECRET
    - OKTA_SCOPE
    - OKTA_TOKEN_URL
    - POLL_INTERVAL_MS
    - POLL_MAX_ATTEMPTS
    - SIGNATURE_API_BASE_URL
    - TRANSPORT
  type: object
env:
  API_BASE_URL: ""
  FULL_FLOW_EMAIL_BASE: ""
  FULL_FLOW_FILE_PATH: ""
  HTTP_PORT: ""
  OKTA_CLIENT_ID: ""
  OKTA_SCOPE: ""
  OKTA_TOKEN_URL: ""
  POLL_INTERVAL_MS: ""
  POLL_MAX_ATTEMPTS: ""
  SIGNATURE_API_BASE_URL: ""
  TRANSPORT: ""
runtime: typescript

